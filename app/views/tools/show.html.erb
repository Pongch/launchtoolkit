<p id="notice"><%= notice %></p>

<div class="container-fluid hero-image">
    <div class="container hero-text top-padding">
         <h1 class="text-center"><%= @tool.title %> <i class="fa fa-wrench" aria-hidden="true"></i></h1>
    </div>
</div>

<div class="container">
    <br>
    <div class="col-sm-offset-4 col-sm-4">
    <div class="panel panel-default">
        
    <%= image_tag @tool.image.url(:thumb), class:"center-block margin-top" %>
    <p class="text-center margin-top">What it is  for: <span class="label label-danger text-center"><%= @tool.category.name %></span></p>
    <h4 class="text-center margin-top">What it does:</h4>
    <p class="text-center"><%= @tool.description %></p>
    <a target="_blank" href="<%= @tool.link %>">
        <button class="btn-new center-block margin-top">Give <%= @tool.title %> a try!</button>
    </a>
    <br>
    </div>
    </div>
</div>

